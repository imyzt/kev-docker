# 复制为 .env 后按需修改；不配置则使用下方默认值
# 模型服务宿主机监听地址
KEV_PORT=127.0.0.1:8009
# 演示页面宿主机监听地址
WEB_PORT=127.0.0.1:8083
# 基础模型 HF 缓存目录（挂载进容器供只读使用）
HF_CACHE_DIR=$HOME/.cache/huggingface
# CPU 线程数：0.5B 小模型在多容器 VM 上线程越多越慢，实测 1 线程最优
OMP_NUM_THREADS=1